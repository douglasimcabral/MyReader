			<div class="row">
				<div class="col-lg-6">
				</div>
				<div class="col-lg-6">
				  <p class='text-right'>
    <%= link_to "Sair", logout_path, method: "delete", class: 'btn btn-default' %></p>
				</div>
			</div>

<h5 id="notice" class='text-warning'><strong><%= notice %></strong></h5>
<%= form_for(@feed) do |f| %>
  <% if @feed.errors.any? %>
    <div id="error_explanation">
      
<div class='alert alert-danger alert-dismissible' role='alert'>
  <button type='button' class='close' data-dismiss='alert' aria-label='Close'><span aria-hidden='true'>&times;</span></button>
  <h4><strong><%= pluralize(@feed.errors.count, 'erro') %>, favor corrigir:</strong></h4>
      <ul>
      <% @feed.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>  
</div>      
    </div>
  <% end %>

			<div class="row">
				<div class="col-lg-2">
				</div>
				<div class="col-lg-8">
  <div class="field">
    <label for="feed_titulo">T&iacute;tulo</label><br />
    <%= f.text_field :title, class: 'form-control' %>
  </div><br />
  <div class="field">
    <label for="feed_url">Url</label><br />
    <%= f.text_field :url, class: 'form-control' %>
  </div>
  <div class="field">
    <input type="hidden" name="feed[user_id]" id="feed_user_id" value="<%= current_user.id %>">
  </div>
  <div class="actions">
    <br /><p class="text-center"><input type="submit" name="commit" value="Cadastrar" class='btn btn-danger' /> <a class='btn btn-default' href='..'>Cancelar</a></p>
  </div>
<% end %>
				</div>
				<div class="col-lg-2">
				</div>
      </div>				